# Criando arrays para operações
a = np.array([1, 2, 3])
b = np.array([4, 5, 6])

# Operações básicas
soma = a + b
subtracao = a - b
multiplicacao = a * b
divisao = a / b

print(soma, subtracao, multiplicacao, divisao)
